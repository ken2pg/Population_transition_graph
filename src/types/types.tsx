export type prefectureList = { prefCode: number; prefName: string }[];

export type data = {
    label: string;
    data: number[];
    isDisplay: boolean;
    backgroundColor: string;
    borderColor: string;
}[];
