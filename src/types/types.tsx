export type prefectureList = { prefCode: number; prefName: string }[];

export type data = {
    label: string;
    data: number[];
    fill: boolean;
    backgroundColor: string;
    borderColor: string;
}[];
